This directory contains an in-progress Rust re-write of saltybox.

When the re-write is complete, the repo contents will be replaced to
only contain the Rust version (which will be backwards compatible
with the Go version in on-disk format).

The code here should not be used - it represents in-flight work.
